<x-guest-layout><h1>Recupera tu acceso</h1><p class="intro">Ingresa tu correo o C.I. Enviaremos un enlace al correo registrado en tu cuenta.</p><form method="POST" action="{{ route('password.email') }}">@csrf<div class="field"><label for="email">Correo electrónico o C.I.</label><input id="email" name="email" value="{{ old('email') }}" required autocomplete="username"></div><button class="btn btn-primary">Enviar enlace de recuperación</button></form><p class="footnote">Si no tienes correo registrado, comunícate con el taller para restablecer tu contraseña.</p><a class="link" style="display:block;text-align:center;margin-top:22px" href="{{ route('login') }}">Volver a iniciar sesión</a></x-guest-layout>
