<x-guest-layout><span class="eyebrow muted">CONSULTA EN LÍNEA</span><h1 style="margin-top:12px">¿Cómo va tu equipo?</h1><p class="intro">Usa los datos de tu comprobante para consultar el avance del servicio.</p><form method="POST" action="{{ route('seguimiento.buscar') }}">@csrf<div class="field"><label for="orden">Número de orden</label><input id="orden" type="number" name="orden" min="1" value="{{ old('orden') }}" required placeholder="Ej. 1024"></div><div class="field"><label for="ci">Cédula de identidad del titular</label><input id="ci" name="ci" value="{{ old('ci') }}" required maxlength="30" placeholder="Ingresa tu C.I."></div><button class="btn btn-primary"><x-icon name="search"/>Consultar estado</button></form><p class="footnote">También puedes escanear el QR de tu comprobante.</p><div class="guest-rule"></div><a class="btn btn-secondary" href="{{ route('login') }}">Volver al inicio de sesión</a></x-guest-layout>
